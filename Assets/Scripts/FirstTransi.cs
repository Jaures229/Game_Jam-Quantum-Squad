using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstTransi : MonoBehaviour
{
    public void StartFirstTransition()
    {
        /*if (SceneLoader.Instance != null)
        {
            //SceneLoader.Instance.StartLoading("MainScene1");
        }
        else
        {
            Debug.LogError("SceneLoader.Instance est NULL ! Vérifie qu'il est bien dans la première scène.");
        }*/
    }
}
